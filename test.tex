\documentclass[10.5pt]{article}
\input{Languages_config}
\input{Math_config} % 数学配置
% Packages
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta}

% TikZ styles
\tikzset{
    startstop/.style={rectangle, rounded corners, minimum width=3cm, minimum height=1cm, draw=black, fill=blue!15, font=\sffamily},
    process/.style={rectangle, minimum width=3cm, minimum height=1cm, draw=black, fill=green!15, font=\sffamily},
    decision/.style={diamond, aspect=2, minimum width=3.5cm, minimum height=1.5cm, draw=black, fill=orange!20, font=\sffamily, align=center},
    arrow/.style={thick, ->, >=Stealth}
}

\begin{document}

% TikZ picture
\begin{tikzpicture}[
    node distance=4cm,
    every node/.style={draw, rectangle, minimum height=1cm, minimum width=2.8cm, align=center},
    arrow/.style={->, thick, >=Stealth}
]

% Nodes
\node (A) {$\hat{H}$};
\node (B) [right=of A] {$\symbfit{H}$};

% Arrow with label
\draw[arrow] (A) -- node[above] {$H_{ij} = \bra{i}\hat{H}\ket{j}$} (B);

\end{tikzpicture}

\end{document}